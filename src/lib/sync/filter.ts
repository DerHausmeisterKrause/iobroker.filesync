import {minimatch} from "minimatch";import type {FileMetadata,Job} from "../types";
export function matches(file:FileMetadata,filters:Job["filters"]){if(file.type!=="file"||/(^|\/)\.[^/]*\.filesync-(?:[^/]+\.tmp|backup-[^/]+|metadata-[^/]+)$/.test(file.path))return false;if(filters.minSize!==undefined&&file.size<filters.minSize)return false;if(filters.maxSize!==undefined&&file.size>filters.maxSize)return false;const opts={dot:true,nocase:false,matchBase:true};return(filters.include.length===0||filters.include.some(x=>minimatch(file.path,x,opts)))&&!filters.exclude.some(x=>minimatch(file.path,x,opts))}
export function changed(a:FileMetadata,b?:FileMetadata){return !b||a.size!==b.size||Math.floor(a.mtimeMs)!==Math.floor(b.mtimeMs)}
