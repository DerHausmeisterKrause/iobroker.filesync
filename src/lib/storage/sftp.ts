import SftpClient from "ssh2-sftp-client";import path from "node:path";import {createHash,timingSafeEqual} from "node:crypto";import type {FileMetadata,SecretRecord,SftpLocation} from "../types";import type {StorageProvider} from "./provider";import {normalizeRelative} from "../security/path";import{isNotFoundError}from"./errors";
export class SftpStorageProvider implements StorageProvider{private c=new SftpClient();constructor(private readonly l:SftpLocation,private readonly secret:SecretRecord){}normalizePath(p:string){return normalizeRelative(p)}private p(p:string){return path.posix.join(this.l.basePath,"/",normalizeRelative(p))}async connect(){if(!this.l.allowInsecureHostKey&&!this.l.hostFingerprint)throw new Error("SFTP host fingerprint is required");const expected=this.l.hostFingerprint.replace(/^SHA256:/,"");await this.c.connect({host:this.l.host,port:this.l.port,username:this.l.username,password:this.l.auth==="password"?this.secret.password:undefined,privateKey:this.l.auth==="privateKey"?this.secret.privateKey:undefined,passphrase:this.secret.passphrase,readyTimeout:this.l.timeoutMs,hostVerifier:this.l.allowInsecureHostKey?()=>true:key=>{const actual=createHash("sha256").update(key).digest("base64").replace(/=+$/,"");const a=Buffer.from(actual),e=Buffer.from(expected.replace(/=+$/,""));return a.length===e.length&&timingSafeEqual(a,e)}})}async disconnect(){await this.c.end()}async testConnection(){const t=Date.now();await this.list("");return{latencyMs:Date.now()-t}}async list(p:string):Promise<FileMetadata[]>{return(await this.c.list(this.p(p))).map(x=>({path:path.posix.join(this.normalizePath(p),x.name),name:x.name,type:x.type==="d"?"directory":x.type==="l"?"symlink":"file",size:x.size,mtimeMs:x.modifyTime}))}async stat(p:string){const s=await this.c.stat(this.p(p));return{path:this.normalizePath(p),name:path.posix.basename(p),type:s.isDirectory?"directory":"file",size:s.size,mtimeMs:s.modifyTime} as FileMetadata}async statOrUndefined(p:string){try{return await this.stat(p)}catch(error){if(isNotFoundError(error))return undefined;throw error}}async exists(p:string){return (await this.statOrUndefined(p))!==undefined}async mkdir(p:string){if(this.l.readOnly)throw new Error("Location is read-only");await this.c.mkdir(this.p(p),true)}async createReadStream(p:string){return this.c.createReadStream(this.p(p))}async createWriteStream(p:string){if(this.l.readOnly)throw new Error("Location is read-only");return this.c.createWriteStream(this.p(p))}async rename(a:string,b:string){await this.c.rename(this.p(a),this.p(b))}async replace(temp:string,final:string){const backup=`${final}.filesync-backup`;const existed=await this.exists(final);if(existed)await this.rename(final,backup);try{await this.rename(temp,final);if(existed)await this.remove(backup)}catch(e){if(existed&&await this.exists(backup))await this.rename(backup,final);throw e}}async remove(p:string){const s=await this.stat(p);if(s.type==="directory")await this.c.rmdir(this.p(p),true);else await this.c.delete(this.p(p))}async setMtime(p:string,m:number){await this.c.utimes(this.p(p),m/1000,m/1000)}}
