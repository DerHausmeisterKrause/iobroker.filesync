export async function retry<T>(operation:()=>Promise<T>,options:{attempts:number;baseDelayMs:number;exponential:boolean;maxDelayMs:number},sleep=(ms:number)=>new Promise<void>(r=>setTimeout(r,ms))):Promise<T>{let last:unknown;for(let i=0;i<=options.attempts;i++){try{return await operation()}catch(e){last=e;if(i===options.attempts)break;const delay=Math.min(options.maxDelayMs,options.baseDelayMs*(options.exponential?2**i:1));await sleep(delay)}}throw last}
