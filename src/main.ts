/* eslint-disable @typescript-eslint/no-explicit-any */
import * as utils from "@iobroker/adapter-core";import path from "node:path";import { hashPassword } from "./lib/web-auth";import { StandaloneWebServer } from "./lib/web-server";import type{AdapterConfig,DataLocation,Job,SecretRecord}from "./lib/types";import{migrateConfig,secureLocationCredentialId,validateJob,validateLocation,validateRelations,validateSecret}from "./lib/config";import{providerFor}from "./lib/storage/factory";import{PersistentStore}from "./lib/persistence/store";import{SyncEngine}from "./lib/sync/engine";import{RunManager,type RunSource}from "./lib/runs/manager";import{TransferLimiter}from "./lib/jobs/transfer-limit";import{redact}from "./lib/security/redact";import{assertAdminTransport,type ControllerMessageEnvelope}from "./lib/permissions/principal";import{CredentialStore,RuntimeConfigStore}from "./lib/runtime-config-store";import{localLinksEqual,localLinksForWeb}from "./lib/local-links";
type Request={id?:string;locationId?:string;path?:string;writeTest?:boolean;location?:unknown;job?:unknown;secret?:SecretRecord;deleteSecret?:boolean;dryRun?:boolean;offset?:number;limit?:number};
export class FileSyncAdapter extends utils.Adapter{private cfg!:AdapterConfig;private webServer?:StandaloneWebServer;private secrets:Record<string,SecretRecord>={};private runtimeStore!:RuntimeConfigStore;private credentialStore!:CredentialStore;private mutationQueue:Promise<void>=Promise.resolve();private engine!:SyncEngine;private transfers!:TransferLimiter;private runManager!:RunManager;private timers=new Map<string,NodeJS.Timeout>();private failedJobs=new Set<string>();private health?:NodeJS.Timeout;private stopping=false;constructor(options:Partial<utils.AdapterOptions>={}){super({...options,name:"filesync"});this.on("ready",()=>void this.ready().catch(e=>this.log.error(this.redactSafe(e))));this.on("message",obj=>void this.message(obj).catch(e=>this.log.error(this.redactSafe(e))));this.on("stateChange",(id,state)=>void this.state(id,state).catch(e=>this.log.warn(this.redactSafe(e))));this.on("unload",cb=>void this.unload(cb).catch(e=>{this.log.error(this.redactSafe(e));cb()}))}
 private async ready(){
  await this.ensureInfoStates();
  await this.setStateAsync("info.connection",false,true);
  try{
   this.cfg=migrateConfig(this.config as unknown as Partial<AdapterConfig>);
   if(!this.cfg.web.enabled)await this.syncLocalLinks(false);
   const instanceDataDir=utils.getAbsoluteInstanceDataDir(this);
   this.runtimeStore=new RuntimeConfigStore(instanceDataDir);
   this.credentialStore=new CredentialStore(instanceDataDir,value=>this.encrypt(value),value=>this.decrypt(value));
   await this.loadRuntimeConfiguration();
   await this.processWebUsers();
   this.runManager=new RunManager(path.join(utils.getAbsoluteInstanceDataDir(this),"history"),error=>this.redactSafe(error),this.cfg.auditRetention,20);await this.runManager.load();
   this.transfers=new TransferLimiter(this.cfg.maxConcurrentTransfers,()=>{void this.updateCounters().catch(e=>this.log.debug(this.redactSafe(e)))});
   this.engine=new SyncEngine(new PersistentStore(path.join(utils.getAbsoluteInstanceDataDir(this),"indexes")),Date.now,25,operation=>this.transfers.run(operation),e=>this.log.error(`Snapshot checkpoint failed: ${this.redactSafe(e)}`));
   await this.subscribeStatesAsync("jobs.*.trigger");await this.subscribeStatesAsync("jobs.*.enabled");await this.rebuildSchedule();this.health=setInterval(()=>void this.checkHealth(),this.cfg.healthIntervalSeconds*1000);
   if(this.cfg.web.enabled){if(this.hasWebAdmin())await this.startWebServer().catch(error=>this.log.error(`Webserver disabled/not started: ${this.redactSafe(error)}`));else this.log.warn("Webserver disabled/not started: no active web administrator configured")}
   await this.setStateAsync("info.connection",true,true);await this.updateCounters()
  }catch(e){this.log.error(this.redactSafe(e));await this.setStateAsync("info.connection",false,true)}
 }
 private hasWebAdmin(){return this.cfg.webUsers.some(user=>user.enabled&&user.admin&&user.passwordHash)}
 private async ensureInfoStates(){
  const states=[
   ["info.connection","Connected","boolean","indicator.connected"],["info.activeJobs","Active jobs","number","value"],["info.failedJobs","Failed jobs","number","value"],["info.queuedTransfers","Queued transfers","number","value"],
   ["info.webServerRunning","Web server running","boolean","value"],["info.webServerPort","Web server port","number","value"],["info.webServerSecure","Web server secure","boolean","value"],["info.webServerUrl","Web server URL","string","value"]
  ] as const;
  for(const [id,name,type,role]of states)await this.setObjectNotExistsAsync(id,{type:"state",common:{name,type,role,read:true,write:false},native:{}});
  await Promise.all([this.setStateAsync("info.webServerRunning",false,true),this.setStateAsync("info.webServerPort",Number((this.config as any).web?.port??8095),true),this.setStateAsync("info.webServerSecure",Boolean((this.config as any).web?.secure),true),this.setStateAsync("info.webServerUrl","",true)])
 }
 private async processWebUsers(){let changed=false;for(const user of this.cfg.webUsers){if(user.newPassword){user.passwordHash=await hashPassword(user.newPassword);delete user.newPassword;changed=true}}if(changed)await this.persistStaticConfig()}
 private async syncLocalLinks(running:boolean){const id=`system.adapter.${this.namespace}`,object=await this.getForeignObjectAsync(id),wanted=localLinksForWeb(this.cfg.web,running);if(!localLinksEqual(object?.common?.localLinks,wanted))await this.extendForeignObjectAsync(id,{common:{localLinks:wanted}} as ioBroker.PartialObject)}
 private async startWebServer(){const webRoot=path.join(__dirname,"..","web-dist");this.webServer=new StandaloneWebServer({config:()=>this.cfg,publicLocation:l=>this.publicLocation(l),persistLocation:(location,secret)=>this.saveLocation({location,secret}),deleteLocation:async id=>{await this.deleteLocation(id)},persistJob:job=>this.saveJob(job),deleteJob:async id=>{await this.deleteJob(id)},testLocation:(id,write)=>this.withProvider(id,p=>p.testConnection(write)),browseLocation:async(id,requestedPath,offset,limit)=>{const entries=(await this.withProvider(id,p=>p.list(requestedPath))).filter(x=>x.type==="directory");return{path:requestedPath,offset,limit,total:entries.length,hasMore:offset+limit<entries.length,entries:entries.slice(offset,offset+limit)}},startRun:(id,preview)=>this.startRun(id,preview),run:id=>this.runManager.get(id),runs:limit=>this.runManager.history(limit),runItems:(id,offset,limit)=>this.runManager.getItems(id,offset,limit),status:()=>({version:"0.1.0",connected:true,webServer:true,activeJobs:this.runManager.activeCount,failedJobs:this.failedJobs.size,queue:this.transfers.pendingCount,locations:this.cfg.locations.length,jobs:this.cfg.jobs.length}),tls:()=>this.loadTls()},webRoot,this.cfg.web.secure,this.cfg.web.sessionTtlMinutes);try{await this.webServer.start(this.cfg.web.port,this.cfg.web.bind);const host=this.cfg.web.bind==="0.0.0.0"?"HOST":this.cfg.web.bind,url=`${this.cfg.web.secure?"https":"http"}://${host}:${this.cfg.web.port}`;await Promise.all([this.setStateAsync("info.webServerRunning",true,true),this.setStateAsync("info.webServerUrl",url,true),this.syncLocalLinks(true)]);if(!this.cfg.web.secure)this.log.warn("FileSync HTTP login is unencrypted; use it only on a trusted LAN") }catch(error){await Promise.all([this.setStateAsync("info.webServerRunning",false,true),this.syncLocalLinks(false)]);throw new Error(`FileSync web server could not listen on ${this.cfg.web.bind}:${this.cfg.web.port}: ${this.redactSafe(error)}`)}}
 private async loadTls(){const{certPublic,certPrivate,certChained}=this.cfg.web;if(!certPublic||!certPrivate)throw new Error("HTTPS requires public and private ioBroker certificates");const [certificates]=await this.getCertificatesAsync(certPublic,certPrivate,certChained);if(!certificates.key||!certificates.cert)throw new Error("Selected ioBroker TLS certificates are incomplete");return certificates}
 private auth(envelope:ControllerMessageEnvelope){assertAdminTransport(envelope)} private redactSafe(error:unknown){return String(redact(error,Object.values(this.secrets).flatMap(secret=>Object.values(secret).filter((value):value is string=>typeof value==="string"&&value.length>0))))}
 private publicLocation(l:DataLocation){const secret="credentialId"in l?this.secrets[l.credentialId]??{}:{};return{...Object.fromEntries(Object.entries(l).filter(([key])=>key!=="credentialId")),target:l.type==="local"?l.basePath:l.type==="smb"?`${l.host}/${l.share}`:`${l.host}:${l.basePath}`,hasPassword:Boolean(secret.password),hasPrivateKey:Boolean(secret.privateKey),hasPassphrase:Boolean(secret.passphrase)}}
 private async message(obj:ioBroker.Message){if(!obj.command||!obj.callback)return;const r=(obj.message??{})as Request;try{let data:unknown;switch(obj.command){case"listLocations":this.auth(obj);data=this.cfg.locations.map(l=>this.publicLocation(l));break;case"testLocation":this.auth(obj);data=await this.withProvider(r.locationId!,p=>p.testConnection(Boolean(r.writeTest)));break;case"browseLocation":this.auth(obj);{const requestedPath=r.path??"",offset=Math.max(0,r.offset??0),limit=Math.max(1,Math.min(500,r.limit??200)),entries=(await this.withProvider(r.locationId!,p=>p.list(requestedPath))).filter(x=>x.type==="directory");data={path:requestedPath,offset,limit,total:entries.length,hasMore:offset+limit<entries.length,entries:entries.slice(offset,offset+limit)}}break;case"saveLocation":this.auth(obj);if(r.secret||r.deleteSecret)this.auth(obj);data=await this.saveLocation(r);break;case"deleteLocation":this.auth(obj);data=await this.deleteLocation(r.locationId!);break;case"listJobs":this.auth(obj);data=this.cfg.jobs;break;case"saveJob":this.auth(obj);data=await this.saveJob(r.job);break;case"deleteJob":this.auth(obj);data=await this.deleteJob(r.id!);break;case"runJob":this.auth(obj);data=this.startRun(r.id!,false);break;case"previewJob":this.auth(obj);data=this.startRun(r.id!,true);break;case"getRunStatus":this.auth(obj);data=this.runManager.get(r.id!);break;case"getRunHistory":this.auth(obj);data=this.runManager.history(r.limit);break;case"getRunItems":this.auth(obj);data=this.runManager.getItems(r.id!,r.offset,r.limit);break;case"status":this.auth(obj);data={connected:true,activeJobs:this.runManager.activeCount,failedJobs:this.failedJobs.size,queuedTransfers:this.transfers.pendingCount,locations:this.cfg.locations.length,jobs:this.cfg.jobs.length};break;default:throw new Error("Unknown command")}this.sendTo(obj.from,obj.command,{ok:true,data},obj.callback)}catch(e){this.sendTo(obj.from,obj.command,{ok:false,error:this.redactSafe(e)},obj.callback)}}
 private async withProvider<T>(id:string,fn:(p:ReturnType<typeof providerFor>)=>Promise<T>){const l=this.cfg.locations.find(x=>x.id===id);if(!l)throw new Error("Location not found");const p=providerFor(l,this.secrets);await p.connect();try{return await fn(p)}finally{await p.disconnect()}}
 private credentialInUse(id:string,exceptLocationId:string){return this.cfg.locations.some(l=>l.id!==exceptLocationId&&"credentialId"in l&&l.credentialId===id)}
 private cloneConfig(){return structuredClone(this.cfg)}
 private cloneSecrets(){return structuredClone(this.secrets)}
 private serializeMutation<T>(operation:()=>Promise<T>):Promise<T>{const result=this.mutationQueue.then(operation);this.mutationQueue=result.then(()=>undefined,()=>undefined);return result}
 private async loadRuntimeConfiguration(){
  if(await this.runtimeStore.isInitialized()){const runtime=await this.runtimeStore.load();this.cfg.locations=runtime.locations;this.cfg.jobs=runtime.jobs}else await this.runtimeStore.save({version:1,locations:this.cfg.locations,jobs:this.cfg.jobs});
  if(await this.credentialStore.isInitialized())this.secrets=await this.credentialStore.load();else{const vault=this.cfg.credentialVault?.trim();if(vault&&vault!=="{}")try{const parsed:unknown=JSON.parse(vault);if(!parsed||Array.isArray(parsed)||typeof parsed!=="object")throw new Error("invalid vault");this.secrets=parsed as Record<string,SecretRecord>}catch{this.secrets={};this.log.warn("Legacy credential vault was invalid and has been ignored")}await this.credentialStore.save(this.secrets)}
 }
 private async persistStaticConfig(){const adapter=this as unknown as{updateConfig(config:ioBroker.AdapterConfig):Promise<void>};if(typeof adapter.updateConfig!=="function")throw new Error("This js-controller does not provide the secure updateConfig API");await adapter.updateConfig(this.cfg as unknown as ioBroker.AdapterConfig)}
 private async persistRuntime(config:AdapterConfig,secrets:Record<string,SecretRecord>){await this.credentialStore.save(secrets);await this.runtimeStore.save({version:1,locations:config.locations,jobs:config.jobs})}
 private saveLocation(r:Request){return this.serializeMutation(async()=>{const next=this.cloneConfig(),nextSecrets=this.cloneSecrets();const requested=r.location&&typeof r.location==="object"?r.location as{id?:string}:{};const existing=next.locations.find(x=>x.id===requested.id);const l=validateLocation(secureLocationCredentialId(r.location,existing));if(r.secret){r.secret=validateSecret(r.secret);if(!("credentialId"in l))throw new Error("Local locations cannot store credentials");nextSecrets[l.credentialId]={...nextSecrets[l.credentialId],...Object.fromEntries(Object.entries(r.secret).filter(([,v])=>v!==""))}}if(r.deleteSecret&&"credentialId"in l)delete nextSecrets[l.credentialId];if(existing&&"credentialId"in existing&&(!("credentialId"in l)||existing.credentialId!==l.credentialId)&&!next.locations.some(x=>x.id!==existing.id&&"credentialId"in x&&x.credentialId===existing.credentialId))delete nextSecrets[existing.credentialId];const i=next.locations.findIndex(x=>x.id===l.id);if(i<0)next.locations.push(l);else next.locations[i]=l;for(const job of next.jobs.filter(j=>j.sourceLocationId===l.id||j.targetLocationId===l.id))validateRelations(next,job);await this.persistRuntime(next,nextSecrets);this.cfg=next;this.secrets=nextSecrets;await this.rebuildSchedule();return this.publicLocation(l)})}
 private deleteLocation(id:string){return this.serializeMutation(async()=>{if(this.cfg.jobs.some(j=>j.sourceLocationId===id||j.targetLocationId===id))throw new Error("Location is referenced by a job");const next=this.cloneConfig(),nextSecrets=this.cloneSecrets(),l=next.locations.find(x=>x.id===id);next.locations=next.locations.filter(x=>x.id!==id);if(l&&"credentialId"in l&&!next.locations.some(x=>"credentialId"in x&&x.credentialId===l.credentialId))delete nextSecrets[l.credentialId];await this.persistRuntime(next,nextSecrets);this.cfg=next;this.secrets=nextSecrets;await this.rebuildSchedule();return true})}
 private saveJob(raw:unknown){return this.serializeMutation(async()=>{const j=validateJob(raw),next=this.cloneConfig(),nextSecrets=this.cloneSecrets();const i=next.jobs.findIndex(x=>x.id===j.id);if(i<0)next.jobs.push(j);else next.jobs[i]=j;validateRelations(next,j);const{relationWarnings}=await import("./lib/config");for(const warning of relationWarnings(next.jobs))this.log.warn(warning);await this.persistRuntime(next,nextSecrets);this.cfg=next;this.secrets=nextSecrets;await this.rebuildSchedule();return j})}
 private deleteJob(id:string){return this.serializeMutation(async()=>{const next=this.cloneConfig(),nextSecrets=this.cloneSecrets();next.jobs=next.jobs.filter(x=>x.id!==id);await this.persistRuntime(next,nextSecrets);this.cfg=next;this.secrets=nextSecrets;this.failedJobs.delete(id);await this.rebuildSchedule();await this.updateCounters();return true})}
 private async rebuildSchedule(){for(const t of this.timers.values())clearInterval(t);this.timers.clear();for(const j of this.cfg.jobs){await this.ensureJobStates(j);await this.setStateAsync(`jobs.${j.id}.enabled`,j.enabled,true);if(!j.enabled)this.failedJobs.delete(j.id);else if((await this.getStateAsync(`jobs.${j.id}.status`))?.val==="error")this.failedJobs.add(j.id);if(j.enabled&&j.trigger.type!=="manual")this.timers.set(j.id,setInterval(()=>{void Promise.resolve().then(()=>this.startRun(j.id,false,j.trigger.type==="change"?"reconciliation":"schedule")).catch(e=>this.log.warn(`Scheduled job ${j.id} failed: ${this.redactSafe(e)}`))},j.trigger.intervalSeconds*1000))}}
 private async ensureJobStates(j:Job){for(const [key,type,write]of [["enabled","boolean",true],["running","boolean",false],["status","string",false],["lastRun","number",false],["lastSuccess","number",false],["lastError","string",false],["lastDryRun","number",false],["lastDryRunStatus","string",false],["lastDryRunError","string",false],["lastDryRunResultId","string",false],["lastDryRunScanned","number",false],["lastDryRunWouldCopy","number",false],["lastDryRunWouldOverwrite","number",false],["lastDryRunWouldVersion","number",false],["lastDryRunWouldDelete","number",false],["lastDryRunWouldMove","number",false],["lastDryRunBytes","number",false],["filesScanned","number",false],["filesCopied","number",false],["filesSkipped","number",false],["filesFailed","number",false],["bytesCopied","number",false],["duration","number",false],["currentFile","string",false],["queueSize","number",false],["trigger","boolean",true]]as const)await this.setObjectNotExistsAsync(`jobs.${j.id}.${key}`,{type:"state",common:{name:`${j.name} ${key}`,type,role:key==="trigger"?"button":"value",read:true,write},native:{}})}
 private startRun(jobId:string,preview:boolean,source:RunSource=preview?"preview":"manual"){if(this.stopping)throw new Error("Adapter is stopping");const job=this.cfg.jobs.find(x=>x.id===jobId);if(!job||!job.enabled)throw new Error("Job unavailable");const dryRun=job.dryRun||preview;return this.runManager.enqueue(jobId,source,dryRun,runId=>this.run(jobId,runId,dryRun))}
 private async run(id:string,runId:string,effectiveDryRun:boolean){if(this.stopping)throw new Error("Adapter is stopping");const j=this.cfg.jobs.find(x=>x.id===id);if(!j||!j.enabled)throw new Error("Job unavailable");const start=Date.now();await this.setStateAsync(`jobs.${id}.running`,true,true);if(!effectiveDryRun)await this.setStateAsync(`jobs.${id}.status`,"scanning",true);try{const source=this.cfg.locations.find(x=>x.id===j.sourceLocationId)!,target=this.cfg.locations.find(x=>x.id===j.targetLocationId)!,result=await this.engine.run(j,providerFor(source,this.secrets),providerFor(target,this.secrets),effectiveDryRun);if(effectiveDryRun){await Promise.all([this.setStateAsync(`jobs.${id}.lastDryRun`,Date.now(),true),this.setStateAsync(`jobs.${id}.lastDryRunStatus`,"success",true),this.setStateAsync(`jobs.${id}.lastDryRunError`,"",true),this.setStateAsync(`jobs.${id}.lastDryRunResultId`,runId,true),this.setStateAsync(`jobs.${id}.lastDryRunScanned`,result.scanned,true),this.setStateAsync(`jobs.${id}.lastDryRunWouldCopy`,result.copied-result.overwritten-result.versioned,true),this.setStateAsync(`jobs.${id}.lastDryRunWouldOverwrite`,result.overwritten,true),this.setStateAsync(`jobs.${id}.lastDryRunWouldVersion`,result.versioned,true),this.setStateAsync(`jobs.${id}.lastDryRunWouldDelete`,result.deleted,true),this.setStateAsync(`jobs.${id}.lastDryRunWouldMove`,result.moved,true),this.setStateAsync(`jobs.${id}.lastDryRunBytes`,result.bytes,true)])}else{await Promise.all([this.setStateAsync(`jobs.${id}.status`,"success",true),this.setStateAsync(`jobs.${id}.lastSuccess`,Date.now(),true),this.setStateAsync(`jobs.${id}.filesScanned`,result.scanned,true),this.setStateAsync(`jobs.${id}.filesCopied`,result.copied,true),this.setStateAsync(`jobs.${id}.filesSkipped`,result.skipped,true),this.setStateAsync(`jobs.${id}.filesFailed`,result.failed,true),this.setStateAsync(`jobs.${id}.bytesCopied`,result.bytes,true)]);this.failedJobs.delete(id)}return{summary:{scanned:result.scanned,copied:result.copied-result.overwritten-result.versioned,overwritten:result.overwritten,versioned:result.versioned,moved:result.moved,deleted:result.deleted,skipped:result.skipped,stabilityDeferred:result.stabilityDeferred,bytes:result.bytes,totalActions:result.totalActions,truncated:result.resultTruncated},items:result.items,total:result.totalActions}}catch(error){const safe=this.redactSafe(error);if(effectiveDryRun)await Promise.all([this.setStateAsync(`jobs.${id}.lastDryRun`,Date.now(),true),this.setStateAsync(`jobs.${id}.lastDryRunStatus`,"error",true),this.setStateAsync(`jobs.${id}.lastDryRunError`,safe,true),this.setStateAsync(`jobs.${id}.lastDryRunResultId`,runId,true)]);else{await this.setStateAsync(`jobs.${id}.status`,"error",true);await this.setStateAsync(`jobs.${id}.lastError`,safe,true);await this.setStateAsync(`jobs.${id}.filesFailed`,1,true);this.failedJobs.add(id)}throw error}finally{if(!effectiveDryRun){await this.setStateAsync(`jobs.${id}.lastRun`,Date.now(),true);await this.setStateAsync(`jobs.${id}.duration`,Date.now()-start,true)}await this.setStateAsync(`jobs.${id}.running`,false,true);await this.updateCounters()}}
 private async state(id:string,state:ioBroker.State|null|undefined){if(!state||state.ack)return;const jobId=id.split(".").at(-2);if(id.endsWith(".trigger")&&state.val===true){if(jobId)try{this.startRun(jobId,false,"state")}catch(error){this.log.warn(this.redactSafe(error))}await this.setStateAsync(id,false,true)}else if(id.endsWith(".enabled")&&typeof state.val==="boolean"&&jobId){const current=this.cfg.jobs.find(x=>x.id===jobId);if(!current)return;const next=this.cloneConfig(),nextSecrets=this.cloneSecrets(),candidate=next.jobs.find(x=>x.id===jobId)!;candidate.enabled=state.val;candidate.updatedAt=new Date().toISOString();try{await this.persistRuntime(next,nextSecrets);this.cfg=next;this.secrets=nextSecrets;if(!candidate.enabled)this.failedJobs.delete(candidate.id);await this.rebuildSchedule();await this.updateCounters()}catch(error){await this.setStateAsync(id,current.enabled,true);throw error}}}private async updateCounters(){await this.setStateAsync("info.activeJobs",this.runManager?.activeCount??0,true);await this.setStateAsync("info.failedJobs",this.failedJobs.size,true);await this.setStateAsync("info.queuedTransfers",this.transfers?.pendingCount??0,true)}private async checkHealth(){for(const l of this.cfg.locations.filter(x=>x.enabled)){await this.withProvider(l.id,p=>p.testConnection()).catch(e=>this.log.debug(`Health check failed for ${l.id}: ${this.redactSafe(e)}`))}}
 private async unload(cb:()=>void){this.stopping=true;for(const t of this.timers.values())clearInterval(t);this.timers.clear();if(this.health)clearInterval(this.health);this.transfers?.close();await this.webServer?.stop().catch(e=>this.log.warn(this.redactSafe(e)));await this.setStateAsync("info.webServerRunning",false,true).catch(()=>undefined);await this.runManager?.shutdown(30000);await this.transfers?.waitForIdle(30000);await this.setStateAsync("info.connection",false,true).catch(()=>undefined);cb()}}
if(require.main===module)new FileSyncAdapter();export default(options:Partial<utils.AdapterOptions>={})=>new FileSyncAdapter(options);
